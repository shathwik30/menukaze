{
  "name": "menukaze",
  "version": "0.0.0",
  "private": true,
  "description": "Menukaze — multi-tenant restaurant SaaS. Monorepo root.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "services:up": "docker-compose up -d --wait mongo redis",
    "services:down": "docker-compose down",
    "services:reset": "docker-compose down -v && docker-compose up -d --wait mongo redis",
    "services:logs": "docker-compose logs -f",
    "db:seed": "tsx packages/db/scripts/seed.ts",
    "db:migrate": "tsx packages/db/scripts/migrate.ts up",
    "prepare": "husky"
  },
  "devDependencies": {
    "turbo": "2.9.6",
    "typescript": "5.9.3",
    "prettier": "3.8.2",
    "prettier-plugin-tailwindcss": "0.6.14",
    "husky": "9.1.7",
    "lint-staged": "15.5.2"
  }
}
